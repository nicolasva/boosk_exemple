#store-settings
  #new_shipping_method
    .container
      %h2.span12= t :h2, :scope => [:views, :shipping_method, :new]
      = form_for [@shop, @shipping_method], :id => "new_shipping_method_form" do |f|
        = render 'form', shipping_method: @shipping_method, f: f